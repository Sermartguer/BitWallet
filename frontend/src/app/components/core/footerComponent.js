import React from 'react';

// The Header creates links that can be used to navigate
// between routes.
const Footer = () => (
  <footer>
    <span>Footer</span>
  </footer>
);

export default Footer;
